# sub2gen Google Flow Provider

The canonical home for Google Flow's provider-SDK adapter and reusable upstream
capability resources. Application orchestration, credentials, token selection,
CAPTCHA handling, caching, and logging remain owned by the API application.
