{
  "name": "@sub2gen/extension-core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "bun run check",
    "check": "bun run typecheck && bun run test",
    "typecheck": "tsc --noEmit --pretty false",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "~6.0.3",
    "vitest": "^4.1.0"
  }
}
