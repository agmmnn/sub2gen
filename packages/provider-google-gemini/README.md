# sub2gen provider: Google Gemini

Direct Gemini image generation adapter for the provider SDK. It is independent from
the older GeminiGen browser service and accepts credentials only when its backend is
constructed.
